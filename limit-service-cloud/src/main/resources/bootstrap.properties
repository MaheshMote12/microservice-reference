#this property does is, it locates config server from discovery client. ie eureka
spring.cloud.config.discovery.enabled=true
spring.cloud.config.name=limit-service-cloud
spring.cloud.config.discovery.service-id=config-server
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

spring.cloud.config.profile=dev

#we need to disable this because we are not  locating config server from discovery and not hard coding uri
#spring.cloud.config.uri=http://localhost:8888

